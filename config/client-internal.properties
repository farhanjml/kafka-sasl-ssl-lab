# client-internal.properties
# Used by kafka-* CLI tools to connect via INTERNAL listener (PLAINTEXT)
# Mounted into broker containers at /etc/kafka/client-internal.properties

security.protocol=PLAINTEXT
